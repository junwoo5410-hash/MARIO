# MARIO — 논문·초록 작성용 결과 및 수행 작업 정리

작성 2026-09-08. 브랜치 `no-motor` (`e355083`) 기준.

이 문서는 초록과 본문을 쓸 때 **인용해도 되는 수치**와 **인용하면 안 되는 수치**를
구분해 모아둔 것이다. 각 수치에는 산출 파일을 붙였다. 세 개의 원문 보고서
(`mario_sitl/BLACKBIRD_IMPROVEMENTS.md`, `mario_sitl/TRANSFER_STUDY.md`,
`mario_sitl/REPORT.md`)를 대체하지 않고 그 위에서 교차 검증한 요약이다.

---

## 1. 한 줄 요약

IMU만으로 바디 프레임 변위를 추정하는 causal Mamba 네트워크(MARIO)를
MIT Blackbird에서 학습하고, (a) 학습 신호에 섞여 있던 지름길을 제거해 미관측 궤적
오차를 대폭 낮췄으며, (b) EuRoC로의 전이와 (c) PX4 SITL 폐루프 비행에서
이 계열 모델의 실제 적용 한계를 정량화했다.

**세 개의 독립된 기여로 나눌 수 있다.**

1. **추력 채널은 학습 지름길이었다** — Blackbird의 모터 입력을 제거하면 미관측 궤적
   ATE가 5.361 → 1.122 m로 79 % 줄고, 창 단위 RMSE는 오히려 나빠진다. 국소 지표와
   적분 지표가 반대로 움직이는 것이 계통 편향 제거의 서명이다. (§3)
2. **평가 구간이 문헌 비교를 왜곡한다** — 저장소의 롤아웃 구현이 궤적의 일부만
   평가하고 있었고, 이를 고치자 "양방향 MARIO가 AirIO를 이긴다"는 결론이 뒤집혔다. (§6)
3. **전이는 실패했고, 그 실패를 정량화했다** — EuRoC에서는 어떤 설정도 "드론이 전혀
   움직이지 않았다"고 답하는 자명한 기준선을 넘지 못한다. (§5)

---

## 2. 시스템과 지표 정의

### 2.1 모델

`mario/model.py`. 입력 윈도우 1000 샘플(100 Hz → 10 s).

| 구성 | 내용 |
|---|---|
| 인코더 | 1D CNN 2개 (acc 3ch, gyro 3ch). 각 `Conv1d(k=7, s=3, p=3) → BN → GELU` 2단 + Dropout 0.5 → 시간축 9배 다운샘플 |
| 융합 | 두 인코더 출력 concat(128) → `Linear(128, d_model)` |
| 시퀀스 | pre-norm Mamba 블록 잔차 스택 (`mamba_ssm.Mamba`) |
| 출력 | 변위 헤드 + 공분산 헤드 (`exp(decoder − 5)`) |
| 보조 입력 | 자세를 SO(3) Log로 넣음 (GT 자세 또는 EKF2 자세) |

라벨은 9 샘플 간격, 윈도우 내 인덱스 14부터. 즉 한 윈도우가 90 ms 단위 변위 111개를 낸다.

**파라미터 수**

| 변형 | params | 비고 |
|---|---:|---|
| 모터 ON (원본) | 95,974 | `motor_encoder` 포함 |
| M | 95,974 | 모터 **입력만** 0. 인코더는 그래프에 남아 상수 기여 |
| **NM** (현 브랜치) | **76,582** | 모터 분기를 코드에서 제거 |
| MB | 131,878 | NM 양방향 |
| AirIO 단방향 | 205,638 | 비교 대상 |
| AirIO 양방향 | 387,014 | 비교 대상 (원 논문 구성) |

### 2.2 지표

- **ATE** = `sqrt(mean(‖p̂_t − p_t‖²))`. `mario/evaluate.py:72`와 AirIO
  `evaluate_motion.py:133`이 같은 정의를 쓴다.
- **TDE** = ATE ÷ (GT 이동 거리) × 100 [%].
- **주의**: AirIO가 화면에 찍는 `pos_err`는 오차 노름의 **산술 평균**
  (`velocity_integrator.py:133`)으로 값이 다르다. 혼용 금지.
- 롤아웃은 GT 첫 위치에 **한 번만** 앵커하고 이후 예측 변위만 누적한다
  (윈도우마다 상태는 초기화, 위치는 초기화하지 않음).

### 2.3 데이터

| | Blackbird | EuRoC MAV | PX4 SITL (x500) |
|---|---|---|---|
| 출처 | MIT | ETH ASL, IJRR 2016 | 자체 수집 |
| split | seen 5 / unseen 5 궤적 | 공식 train 6 / test 5 | flight_1..N |
| IMU | 100 Hz | 200 Hz (→ 100 Hz 리샘플) | 100 Hz |
| 속도 | 2.7–5.4 m/s | 0.4–0.5 m/s | 1.2 / 4.3–4.7 m/s |
| gyro std | 0.8–2.3 rad/s | 0.12–0.27 rad/s | — |
| 모터 채널 | z축 집합추력 | 없음 | 4채널 `actuator_motors` |

---

## 3. 핵심 결과 — 추력 채널은 비행 식별자였다

### 3.1 무엇을 발견했나

Blackbird `thrust_data.csv`는 20개 파일 전수 확인 결과 **ch0/ch1이 정확히 0이고 ch2만
활성**인, 질량 정규화된 z축 집합추력이다. `/9.80665` 후 비행별 평균:

| 궤적 | split | 평균 | 표준편차 |
|---|---|---:|---:|
| oval | unseen | −1.225 | 0.050 |
| halfMoon | seen | −1.209 | 0.094 |
| star | seen | −1.192 | 0.157 |
| clover | seen | −1.123 | 0.087 |
| sphinx | unseen | −1.099 | 0.172 |
| winter | seen | −1.084 | 0.057 |
| sid | unseen | −1.035 | 0.042 |
| bentDice | unseen | −1.031 | 0.096 |
| egg | seen | −1.014 | 0.237 |
| ampersand | unseen | −1.006 | 0.048 |

**비행 내 변동(0.04–0.24)이 비행 간 폭(0.22)보다 작다.** 호버 추력 근처의 거의 일정한
값이라 동역학 정보는 빈약한데 "어느 비행인지" 식별력은 강하다 — 예측에는 쓸모없고
암기에는 완벽한 조합이다. 네트워크는 DC 값으로 학습 비행을 판별하고 비행별 보정을
적용한다. 미관측 비행에서는 그 값이 학습 비행 중 하나 근처에 떨어져 **엉뚱한 보정이
매 스텝 같은 방향으로 걸린다.** `oval`(−1.225)과 학습 비행 `halfMoon`(−1.209)이
사실상 같은 DC를 갖는 것이 그 예다.

### 3.2 결정적 증거 — 국소 지표와 적분 지표가 반대로 움직인다

| | 창 단위 RMSE (4시드 범위) | 적분 후 unseen ATE |
|---|---|---:|
| 모터 사용 | 0.01747 (0.01718–0.01777) | 1.5843 |
| 모터 제거 | 0.02020 (0.01990–0.02044) **나빠짐** | 0.9311 (**−41 %**) |

두 RMSE 분포는 시드 4개에서 전혀 겹치지 않는다. **국소 예측이 4/4 시드에서 나빠졌는데
궤적 오차는 4/4 시드에서 좋아졌다.** 지표 아티팩트라면 나올 수 없는 방향이고, 계통 편향
제거의 전형적 서명이다. 동시에 `mario/train.py:108`이 창 RMSE로 `best.pt`를 고르는 것이
왜 이 문제를 잡지 못했는지도 설명한다.

> 위 표는 10.2 초 구간 기준(§6)이다. 방향과 부호가 논지이므로 결론은 유지되지만,
> 절대값은 §3.3의 전체 궤적 수치를 쓸 것.

### 3.3 전체 궤적 4시드 평균 — 인용할 수치

`mario_sitl/results/motor_control_full_rollout.json`, `results/full_rollout.json`

| 설정 | params | seen ATE ±σ | unseen ATE ±σ |
|---|---:|---:|---:|
| 모터 ON (동일 설정 4런) | 95,974 | 7.243 ±0.592 | 5.361 ±0.555 |
| M (모터 입력 0, 인코더 유지) | 95,974 | 0.723 ±0.30 | 1.122 |
| **NM (분기 삭제)** | **76,582** | **0.589 ±0.091** | **1.503 ±0.149** ※ |

- 모터 ON → M: **seen −90.0 %, unseen −79.1 %**
- M → NM: 파라미터 **−20.2 %** (19,392개 = CNN 인코더 15,296 + 융합 Linear의 64×64.
  d_model=64. 이 파라미터들은 상수 입력만 받아 데이터를 한 번도 보지 않았다),
  seen −18.5 %

**※ NM의 unseen 1.503은 M의 1.122보다 나쁜 것이 아니다 — 평가 구간이 다르다.**
기존 `data.py`는 시퀀스를 `thrust_data.csv`의 시간 범위로 클리핑했는데, unseen 비행들의
thrust는 IMU보다 5–6 초 늦게 시작한다(예: ampersand thrust 1534109231.68 vs IMU
1534109225.91). 즉 **unseen 평가에서 앞부분이 계속 잘려 있었다.** thrust를 읽지 않게
되면서 클리핑이 사라져 unseen 시퀀스가 평균 **+29.2 %**(+27.3/+27.6/+34.3/+36.6/+20.3 %)
길어졌고, 같은 체크포인트가 더 긴 궤적을 적분하게 됐다. seen은 범위가 같아 영향이 없다.
**논문에서 M과 NM의 unseen을 나란히 비교하면 안 된다.**

### 3.4 자명한 기준선을 넘는가

`mario_sitl/results/stationary_baseline.json` (본 정리 과정에서 새로 산출)

"드론이 전혀 움직이지 않았다"고 답하는 모델의 ATE — 앵커에서 벗어난 거리의 RMS.
같은 pose 격자에서 계산했다.

| | 정지 기준선 ATE | NM ATE | 배수 |
|---|---:|---:|---:|
| Blackbird seen | 4.623 | 0.589 | **7.8× 우수** |
| Blackbird unseen | 3.519 | 1.503 | **2.3× 우수** |
| EuRoC test | 4.543 | 5.82 (최적 설정) | **넘지 못함** |

**Blackbird 결과는 이 바닥을 확실히 넘고, EuRoC 결과는 넘지 못한다.** §5의 해석은
전적으로 이 사실 위에 서 있다.

### 3.5 범위 제한 (반드시 본문에 쓸 것)

이는 **Blackbird의 이 채널**에 대한 결론이지 "모터 입력이 무용하다"가 아니다. PX4에서
4개 모터 개별 출력을 받으면 기동 중 차동 성분이 실려 성질이 다르다. EuRoC에는 모터
채널이 아예 없으므로, 채널이 빈약해서인지 모터 입력 자체가 무용해서인지는 현 데이터로
가릴 수 없고 **미해결로 남는다.**

---

## 4. AirIO와의 비교

### 4.1 수치

`mario_sitl/results/final_comparison.json` → `full_trajectory`,
`mario_sitl/results/airio_tde.json`

| 설정 | 방향성 | params | seen ATE | seen TDE | unseen ATE | unseen TDE |
|---|---|---:|---:|---:|---:|---:|
| **M — 모터 제거** | 단방향 | 95,974 | 0.723 | 1.245 | **1.122** | **4.036** |
| **MB — 양방향** | 양방향 | 131,878 | **0.551** | **0.967** | 1.126 | 4.238 |
| AirIO 단방향 | 단방향 | 205,638 | 1.032 | 1.427 | 2.285 | 6.957 |
| AirIO 양방향 | 양방향 | 387,014 | 0.464 | 0.647 | 1.294 | 4.077 |

**같은 방향성끼리:**

- **단방향 M vs AirIO 단방향 — seen −29.9 %, unseen ATE −50.9 %, unseen TDE −42.0 %**
- 양방향 MB vs AirIO 양방향 — seen **+18.7 % (진다)**, unseen ATE −12.9 %, TDE +3.9 %

### 4.2 무엇을 주장할 수 있고 없는가

**주장 가능** — 실시간 배포 가능한 단방향 조건에서, MARIO는 파라미터의 절반 이하로
AirIO 단방향 대비 미관측 궤적 오차를 약 절반으로 줄인다.

**주장 불가(철회됨)** — "양방향 MARIO가 AirIO 양방향을 이긴다". 10.2 초 구간에서는
이겼으나(0.3837 vs 0.4644) 전체 궤적에서는 진다(0.551 vs 0.464). 짧은 구간의 산물이었다.

**주장 불가** — 양방향성이 MARIO에서도 중요하다. AirIO는 uni(2.285) vs bi(1.294)로
unseen 43 % 차이지만 MARIO는 causal(0.931) vs bi(0.953)로 사실상 동률이다. Mamba의
상태공간 재귀가 GRU와 달리 단방향에서도 충분한 문맥을 유지한다 — **이것이 실시간 배포
가능한 causal 모델이 살아남는 이유**이고, 이 대비 자체가 결과의 일부다.

### 4.3 남아 있는 비교의 불완전성 (중요, 미해결)

`eval_full_rollout.py`의 보폭 999 수정은 체인 끊김을 고쳤지만 **꼬리는 여전히 버린다.**
윈도우는 1000 샘플이 온전히 남아야 시작할 수 있어, 남은 구간이 그보다 짧으면 평가되지
않는다. 본 정리 과정에서 실측한 커버리지:

| 궤적 | split | seq_len | 전체 거리 | 평가 거리 | 커버 | AirIO 거리 |
|---|---|---:|---:|---:|---:|---:|
| clover | seen | 2998 | 81.7 | 81.2 | 99.4 % | 81.7 |
| halfMoon | seen | 1998 | 57.8 | 29.3 | **50.6 %** | 57.8 |
| star | seen | 1598 | 55.7 | 36.0 | **64.6 %** | 55.7 |
| egg | seen | 2298 | 125.8 | 108.4 | 86.2 % | 125.8 |
| winter | seen | 1998 | 55.2 | 26.9 | **48.6 %** | 55.2 |
| ampersand | unseen | 2689 | 26.7 | 19.9 | 74.7 % | 21.1 |
| sid | unseen | 2032 | 49.9 | 49.2 | 98.5 % | 38.2 |
| oval | unseen | 2033 | 65.0 | 64.4 | 98.9 % | 48.6 |
| sphinx | unseen | 1986 | 62.4 | 32.3 | **51.8 %** | 45.4 |
| bentDice | unseen | 2610 | 61.9 | 47.3 | 76.5 % | 51.5 |

두 가지가 드러난다.

1. **커버리지가 48.6–99.4 %로 궤적마다 다르다.** 적분 오차는 구간이 길수록 쌓이므로
   짧게 평가된 궤적이 유리하다. `BLACKBIRD_IMPROVEMENTS.md` §0의 "AirIO와 동일 구간"
   이라는 서술은 **부정확하다.**
2. **unseen에서는 로더가 만든 시퀀스 길이 자체가 다르다** (sid 49.9 vs AirIO 38.2 m,
   oval 65.0 vs 48.6 m). seen은 일치하므로 seen 비교는 구간 커버리지만 문제이고,
   unseen 비교는 그 위에 원본 구간 차이가 더 있다.

**따라서 §4.1의 unseen 비교는 "동일 구간에서의 비교"로 서술하면 안 된다.** 두 가지
선택지가 있다: (a) 구간 차이를 한계로 명시하고 수치를 그대로 쓰거나, (b) 마지막 윈도우를
시퀀스 끝에 맞춰 붙이도록 롤아웃을 고쳐 다시 측정하는 것. (b)는 아직 하지 않았다.

---

## 5. 전이 연구 — Blackbird → EuRoC

`mario_sitl/TRANSFER_STUDY.md`, `results/transfer/full_rollout_rescore.json`

**주의**: 이 절의 모든 수치는 **M 체크포인트**(`runs/m_s42`, 95,974 params)로 산출했다.
현 브랜치는 모터 분기를 제거해 M 계열 체크포인트가 로드되지 않으므로, 재현하려면
`runs/nm_s42`로 전부 다시 내야 한다.

### 5.1 좌표 규약 — 전이의 첫 관문

정지 시 body-frame 가속도가 두 데이터셋에서 다르다.

| | body 가속도 | 해석 |
|---|---|---|
| Blackbird | `[−0.24, +0.95, −11.19]` | 중력이 −z |
| EuRoC | `[+9.0, 0, −3.6]` | 중력이 **+x**. 70.8° 차이 |

EuRoC의 이 방향은 11개 시퀀스에서 산포 0.53°로 일정하다 — 비행 특성이 아니라 고정
장착이다. 변위 추정은 body 상수 회전에 등변이므로(acc·gyro·라벨을 함께 돌리면 물리가
불변) 상수 회전 정렬은 **좌표 변환이지 적합이 아니다.** 중력 방향만으로는 요가 정해지지
않아 30° 격자로 훑고, 최적값은 **학습 split에서 고르고 테스트에 적용**했다.

| zero-shot | ATE [m] |
|---|---:|
| 정렬 없음 | 21.24 |
| 중력 정렬 (요 0°) | 15.19 |
| 중력 정렬 + 요 185° | **2.61** |

**정렬 전에는 "모델이 원 도메인의 속도를 외웠다"처럼 보인다**(예측/실제 변위 비 7.6배).
정렬 후에는 그 진단이 성립하지 않는다. 전이 실패를 정렬 없이 해석하면 안 된다는 것이
이 실험의 교훈이고, 실제로 이 프로젝트에서 한 번 오진했다.

샘플레이트는 원인이 아니었다. 200 Hz 원본(dt=0.005)을 그대로 넣으면 3.37 m로 학습
분포에 맞춘 100 Hz(2.46 m)보다 나쁘다. 이후 전부 dt=0.01.

### 5.2 파인튜닝 4개 arm — 4시드 평균, 전체 궤적 ATE [m]

동결 모듈은 BatchNorm 통계까지 eval로 고정했다(`module.train`을 no-op으로 치환).
검증은 학습 시퀀스 꼬리 20 %를 1200샘플 간격을 두고 떼어 썼고, 공식 테스트 시퀀스는
학습에도 체크포인트 선택에도 쓰지 않았다.

| arm | 학습 파라미터 | 내용 | ATE ±σ |
|---|---|---|---:|
| `scratch` | 95,974 (100 %) | 무작위 초기화, EuRoC만 | **5.821 ±0.978** |
| `full` | 95,974 (100 %) | 전부, Blackbird 가중치에서 출발 | 5.981 ±0.394 |
| `trunk` | 49,414 (51.5 %) | CNN 인코더 동결, 이후 전부 | 7.709 ±0.995 |
| `head` | 17,414 (18.1 %) | disp/cov 디코더만 = linear probe | 7.760 ±0.196 |
| *zero-shot* | 0 | 학습 없음 | *19.608* |
| **정지 기준선** | — | 변위를 0으로 예측 | **4.543** |

lr은 각 arm이 더 나았던 2.58e-4로 통일. lr 교란 제거를 위해 초기화 × lr 2×2를 돌렸고
낮은 lr에서도 순서가 유지된다(scratch 6.071 < trunk 7.722 ≈ full 7.749 < head 7.958).
**초기화의 효과이지 lr의 효과가 아니다.**

### 5.3 데이터 효율

`results/figures/transfer_data_efficiency.png`

| 학습량 | `scratch` | `head` (linear probe) | `full` |
|---|---:|---:|---:|
| 1.1분 (1 seq) | 23.44 ±11.7 | **13.85 ±0.83** | 82.04 ±1.16 |
| 2.6분 (2 seq) | 19.90 ±2.49 | **16.56 ±0.47** | 61.14 ±24.35 |
| 6.6분 (4 seq) | **6.21 ±0.96** | 7.42 ±0.05 | 9.87 ±0.65 |
| 9.8분 (6 seq) | **5.82 ±0.98** | 7.96 ±0.23 | 7.75 ±0.25 |

`full`은 데이터가 적을 때 최악이다 — 1.1분에서 82.0 m로 zero-shot(19.6)보다도 한참
나쁘다. 사전학습된 특징을 다시 배우기 전에 파괴한다.

### 5.4 결론 — 이 절에서 무엇을 주장할 수 있는가

**주장 불가(철회됨).** "3분 근처에서 linear probe와 scratch가 교차한다", "데이터가
충분하면 사전학습이 짐이 된다" 같은 처방. **모든 EuRoC 설정이 정지 기준선(4.543)을 넘지
못하기 때문에 이 순위는 실패들 사이의 순위다.** 최적 설정 5.821, RMSE 선택 최적 5.196도
모두 기준선 위(=더 나쁨)에 있다.

**주장 가능(그리고 이것이 이 절의 실제 기여다).**

1. **Blackbird에서 학습한 이 계열 모델은 EuRoC로 전이되지 않으며, 9.8분의 목표 도메인
   데이터로 처음부터 학습해도 여전히 자명한 기준선을 넘지 못한다.** 즉 전이 방법의
   문제가 아니라 체제 격차(속도 6배, gyro 10배)의 문제로 보인다.
2. **body 프레임 정렬 없이 전이 실패를 해석하면 오진한다** (21.24 → 2.61, §5.1).
3. **평가 기준선을 명시하지 않은 전이 연구는 실패를 순위로 포장할 수 있다.** 본 연구는
   실제로 그 함정에 한 번 빠졌고, 정지 기준선을 계산한 뒤에야 발견했다. 이 자체가
   방법론적 기여로 서술할 만하다.

---

## 6. 방법론 — 평가 구간 결함

`mario/evaluate.py`의 `rollout_trajectory`는 윈도우를 `window_size`(1000)씩 건너뛴다.
한 윈도우의 라벨은 `s+14`에서 `s+1013`까지인데 다음 윈도우의 첫 라벨은 `s+1014`라
`if t_start in positions` 검사를 통과하지 못하고 **첫 윈도우 이후가 전부 조용히 버려진다.**

- Blackbird eval split: 입력 14,994 샘플 → **113 pose ≈ 10.2 초**, 궤적 거리의 46–71 %
- EuRoC(100–180 초 시퀀스): 10 % 미만

보폭을 999로 두면 `s+1013 = (s+999)+14`로 라벨이 맞물려 체인이 이어진다
(`scripts/eval_full_rollout.py`). 체인 정확성은 오라클 검증으로 확인했다 — GT 변위를
그대로 먹이면 ATE 1.97e-08 ~ 3.58e-07, 150 초 / 73.5 m 시퀀스에서 149.8 초 / 73.3 m를 덮는다.

**이 결함이 실제로 결론을 뒤집었다** (§4.2 양방향 주장 철회). **저장소에 남아 있는
2026-09-06 이전의 모든 MARIO ATE 수치는 10.2 초 구간 기준이다.** 다만 수정 후에도
꼬리 구간은 남아 있다(§4.3).

---

## 7. 시드 프로토콜

이 모델은 학습이 혼돈적이다 — **lr을 0.011 % 바꾸면 seen ATE가 14 % 움직인다.**
같은 시드면 0.08 % 내로 재현되지만, 시드를 바꾸면 baseline unseen TDE가 7.36–7.99로
±8 % 흩어진다.

이 때문에 **단일 시드 결론이 이 프로젝트에서 세 번 뒤집혔다** (B1 −8.0 %, B3 −32.0 %,
"B2가 seen·unseen 동시 개선"). 채택한 규칙:

> 모든 주장은 시드 4개(42/1/2/3) 평균으로 판정하고, 개별 시드 최고값은 배포
> 체크포인트의 실측값으로만 쓴다.

모터 제거는 이 기준을 통과한다 — 4/4 시드에서 개선되고 최소 개선폭이 시드 잡음의 3배
이상이다. 73개 런 중 unseen ATE 상위 8위가 전부 모터 제거 런이고, 모터를 쓴 65개 런은
하나도 그 아래로 내려오지 못했다.

---

## 8. 기각된 가설 — 전부 4시드 또는 4런

| 가설 | 근거 | 결과 |
|---|---|---|
| 검증 궤적 홀드아웃으로 과적합 차단 | `data.py:134`가 선택셋을 cfg.seen에서 만든다 | seen +21 %, unseen +37 % |
| 바디 z축 회전 증강 | 전 비행 yawForward라 기수가 궤적에 묶임 | seen +35 %, unseen +5 % |
| 용량 부족 (170k / 363k) | AirIO가 2–4배 크다 | unseen +18–31 % |
| huber δ 0.05로 RMS 정렬 | MARIO δ=0.002는 사실상 L1 | unseen +24–33 % |
| 양방향성이 AirIO 우위의 원인 | AirIO는 GRU 2단 모두 양방향 | MARIO 양방향은 unseen −3.6 %에 그침 |
| 체크포인트 선택 기준(RMSE vs ATE) | §3.2에서 선택 지표를 의심 | 결론 없음 — 차이가 시드 편차 안 |

홀드아웃 실험의 부산물: **홀드아웃 궤적 RMSE가 epoch 20에 정체한 뒤 100까지 오르지
않는다.** 과적합이 없다는 뜻이고, 이 모델이 데이터/용량 과잉이 아님을 보여준다. 이후
용량 증대도 실패했으므로 병목은 둘 다 아니었다.

---

## 9. 실시간성

### 9.1 순수 추론 지연

`mario_sitl/results/inference_latency_m.json`, `inference_latency_mb.json`
RTX A6000, 윈도우 1000, 매 호출 numpy에서 입력 생성 + host→GPU 복사 포함,
`cuda.synchronize()` 후 측정, 체크포인트 4개 × 3000 iter.

| 아키텍처 | params | mean [ms] | p50 | p95 | p99 |
|---|---:|---:|---:|---:|---:|
| **M / NM (causal)** | 95,974 / 76,582 | **1.776** | 1.766 | 1.824 | **1.853** |
| MB (양방향) | 131,878 | 2.243 | 2.236 | 2.302 | 2.331 |

### 9.2 SITL 실측 (인용 시 이쪽을 쓸 것)

`mario_sitl/REPORT.md` §1

| 항목 | 값 | 기준 |
|---|---:|---|
| 추론율 | **25.0 Hz** (목표 25 Hz) | ≥20 Hz ✅ |
| 종단 지연 p95 | **37.9 ms** | ≤50 ms ✅ |
| 좌표 변환 왕복 오차 | 2.4e-07 m | — |

**"100 Hz 실시간 제어에서 안정 동작"은 SITL 근거로 뒷받침되지 않는다.** 실측된 것은
25 Hz 루프이고 종단 지연 p95가 37.9 ms다. 순수 GPU 추론만 보면 1.78 ms로 100 Hz에
여유가 있으나, 그것은 파이프라인 전체가 아니다. 초록에는 다음 형태를 권한다:

> 단방향 조건에서 미관측 궤적 ATE를 Causal AirIO 대비 50.9 % 감소시켰으며(1.122 vs
> 2.285 m, 시드 4개 평균), 파라미터는 절반 이하(95,974 vs 205,638)다. GPU 추론 지연은
> 평균 1.78 ms / P99 1.85 ms이고, PX4 SITL 폐루프에서 25 Hz 추론과 종단 지연 P95
> 37.9 ms로 동작함을 확인했다.

---

## 10. PX4 SITL 폐루프

`mario_sitl/REPORT.md` 전문. 요약하면 **인프라는 전부 동작하지만 제자리 유지는
불가능하고, 원인은 모델 정확도가 아니라 구조적 관측 불가능성이다.**

### 10.1 물리적 한계 — 저속에서 속도는 관측 불가

가속도계가 재는 것은 비추력(a − g)이다. **등속 운동에서는 a = 0이므로 중력 반작용만
읽힌다** — 호버 중인 기체와 직선 10 m/s 순항 중인 기체의 IMU 출력이 같다. 갈릴레이
상대성이며 센서 품질이나 학습량과 무관하다.

유일한 단서는 **항력**이다. 속도에 비례하는 힘을 버티려 기체가 기울고, 그 기울기가 바디
프레임 수평 비추력으로 남는다. v→0이면 신호도 0이다.

정지 시 가속도계 수평 잡음 0.0277 m/s² 기준:

| 속도 | 항력 신호 (x500) | SNR |
|---:|---:|---:|
| 0.10 m/s | 0.0120 | 0.43 ❌ |
| 0.25 m/s | 0.0300 | 1.08 |
| 1.00 m/s | 0.1200 | 4.34 |
| 4.00 m/s | 0.4800 | 17.35 ✅ |

→ **x500은 0.23 m/s 아래에서 속도가 관측 불가능하다.** Blackbird(항력 3배)는 0.07 m/s.

### 10.2 데이터 수집 설계가 결과를 지배했다

1차 SITL 수집은 셋포인트를 일정 속도로 **걸어서** 옮겨 등속 비행을 만들었고, 그 데이터로
학습한 모델은 **자기 자신의 held-out 비행에서 cos = −0.002**를 기록했다. 도메인 격차로는
나올 수 없는 값이다. 스텝 방식으로 재수집 후:

| | cos | 축별 x/y/z | TDE |
|---|---:|---|---:|
| 걷기 학습 | −0.002 | +0.19/−0.02/+0.20 | 56.5 % |
| **스텝 학습** | **+0.964** | **+0.99/+0.99/+0.97** | **9.70 %** |
| Blackbird 참고 | +0.83 – +0.99 | — | 7.99 % |

정지 시 유령 속도 0.884 → 0.102 m/s (8.7배). **항력이 여전히 Blackbird의 1/3인데도
학습됐다 — 동역학(가속 이벤트의 존재)이 구속 조건이었다.**

### 10.3 폐루프 실패의 구조

**층위 1** — 저속에서 속도 관측 불가(§10.1).
**층위 2** — `EKF2_EV_CTRL=4`는 속도만 융합하고 위치는 적분으로 만든다. 기준점이 없으므로
속도 편향이 아무리 작아도 위치 오차는 시간에 비례해 무제한 커진다.

미션 요구를 역산하면 55 초 동안 0.3 m 이내를 유지하려면 허용 속도 편향은 **0.005 m/s**다.
현재 0.102 m/s이므로 **20배**가 필요하다. 데이터를 10배 더 모아도 2배 수준이고, 편향이
0이어도 적분 발산은 남는다.

**폐루프 고유 양상**: `chase_ratio 0.94` — 기체의 이탈이 추정 오차의 **정확히 반대 방향,
같은 크기**다. 제어기가 틀린 추정에 현실을 맞췄다. 메커니즘은 GPS 기준선과 동일하고
크기만 30배다. **개루프 평가로는 절대 드러나지 않는다.**

### 10.4 Stage 6 — 미션 파라미터는 결과를 설명하지 못한다

10런 전부 `takeoff → land`로 끝났다(제자리 유지 단계 미통과). 상관 분석 (n=10):

| 변수 | r | p |
|---|---:|---:|
| 명령 거리 | +0.123 | 0.735 |
| 호버 시간 | −0.364 | 0.302 |
| 순항 속도 | −0.046 | 0.900 |
| **무장 시점 EKF 오차** | **+0.779** | **0.008** |
| **추정 발산률** | **+0.968** | **<0.00001** |

**추정기 상태가 전부 설명한다.** 무장 게이트가 요구하는 25초 예열 동안 이미 EKF가
1.2–6.8 m 어긋나며, 그 초기 오차가 최종 이탈을 결정한다.

### 10.5 연속 이동 미션 — 드리프트가 유계가 된다

`mario_sitl/results/continuous_run.json`, `continuous_run_yf.json`

제자리 유지 대신 계속 움직이는 미션(반경 8 m, 3.5 m/s 선회)으로 바꾸면:

| | GPS 차단 후 | 비행 거리 | 최종 추정 오차 | 거리 대비 | 드리프트율 |
|---|---:|---:|---:|---:|---:|
| 연속 선회 (3랩) | 43.0 s | 298.2 m | 11.69 m | **3.92 %** | 0.0136 m/s |
| 연속 선회 (yaw-follow) | 43.1 s | 170.3 m | 4.60 m | **2.70 %** | 0.0576 m/s |
| 제자리 유지 (Stage 5) | ~31 s | 1.05 m | 발산 (속도 0.61 → 24.0 m/s) | — | — |

**드리프트가 시간 비례에서 거리 비례로 바뀐다.** 이것이 이 계열 모델의 적정 용도다 —
수십 초 규모의 상대 항법, 그리고 계속 움직이는 미션.

### 10.6 불확실성 헤드는 상수였다

`cov` 헤드가 **입력과 무관한 상수**를 출력했다. 6,747 샘플에서 σ 변동계수 0.000–0.001,
값은 ~0.90 m/s 고정. 원인은 `exp(decoder − 5)`의 **초기값**이 σ_vel 0.9121 m/s이고
실측이 0.898–0.903이라는 것 — 학습이 안 됐다. `uncertainty_weight=1e-4`가
`loss_weight=1000.0`에 맞서 NLL 항의 기울기 기여가 사실상 0이었다.

Stage 5는 이 값을 `VehicleOdometry.velocity_variance`에 넣으므로 **EKF2는 학습된
불확실성의 외양을 쓴 고정 측정 잡음을 받아 왔다.** 치명적이지는 않으나(상수도 유효한
잡음 모델이다) "원리적 가중"은 작동하지 않았다. `uncertainty_weight`를 0.01로 올리면
헤드가 살아난다(변동폭 55.9 %, E[r²/cov] 0.024 → 0.317).

---

## 11. 수행 작업 연표

| 날짜 | 커밋 | 내용 |
|---|---|---|
| 08-26 | `3af552c` | PX4 SITL 스택 구축, Stage 0–4 (환경 게이트, GPS 기준선 0.126 m, 섀도 노드 25 Hz, 좌표계 2.4e-07 m, 개루프 ATE 5.19 m) |
| 08-26 | `884a6c3` | Stage 4b/5 — SITL 파인튜닝, 폐루프 시도 및 실패 원인 |
| 08-27 | `e8464fa` | Stage 4c/5b — 원인은 기체가 아니라 관측성. 스텝 방식 재수집으로 cos −0.002 → +0.964 |
| 09-01 | `729c7d7` | 커스텀 기체 생성기, cov 헤드 결함 규명, Stage 6 스윕 하네스 |
| 09-01 | `af1266b` | Stage 6 완료 — REPORT.md, 상관 분석, 그리드가 실행되지 못한 이유 |
| 09-01 | `b273324` | 3D 궤적 그림 (최선 사례, 체제별 비교) |
| 09-01 | `8668b26` | 연속 이동 폐루프 — 드리프트가 유계로, 발산 60배 감소 |
| 09-02 | `c508a5f` | Blackbird 학습 설정 개선(B1/B2/B3) + 시드 검증. 단일 시드 주장 철회 |
| 09-03 | `09a7ce1` | **모터 채널이 seen 전용 지름길임을 규명, 입력을 0으로** |
| 09-06 | `e355083` | **모터 분기를 `mario/`에서 코드째 제거** (현 브랜치 `no-motor`) |
| 09-04~06 | (미커밋) | EuRoC 전이 연구 — 로더, 정렬, 4 arm × 데이터량 × 시드 매트릭스 |
| 09-08 | (본 정리) | 정지 기준선 산출, 구간 커버리지 실측, 본 문서 |

**커밋되지 않은 산출물**: `mario_sitl/results/transfer/`, `runs/transfer/`,
`mario_sitl/TRANSFER_STUDY.md`(커밋됨), `stationary_baseline.json`,
`scripts/stationary_baseline.py`.

### 11.1 중간에 뒤집힌 판단 (방법론 절에 쓸 만한 것)

1. **"모델이 Blackbird의 속도를 외웠다"** — body 프레임 요 정렬을 하기 전 측정이었다.
   정렬 후 진단이 성립하지 않았다(§5.1).
2. **"양방향 MARIO가 AirIO를 이긴다"** — 10.2 초 구간의 산물(§6).
3. **"EuRoC에서 3분 교차점이 있다"** — 정지 기준선 아래의 순위였다(§5.4).
4. **B1/B3의 단일 시드 개선** — 시드를 바꾸면 부호가 뒤집혔다(§7).
5. **모터 제거의 unseen 개선폭** — 구간 변화가 섞여 있었다(§3.3 ※).

---

## 12. 산출물 인덱스

```
mario/                          모델·데이터·학습·평가 (브랜치 no-motor: 모터 분기 없음)
├── model.py                    CausalMambaDispNet, 76,582 params
├── data.py                     Blackbird 로더 (thrust 미사용)
└── evaluate.py                 rollout_trajectory (구간 결함 있음 — §6)

mario_sitl/
├── BLACKBIRD_IMPROVEMENTS.md   §3, §4, §7, §8의 원문
├── TRANSFER_STUDY.md           §5의 원문
├── REPORT.md                   §10의 원문
├── scripts/
│   ├── train_blackbird_v2.py   Blackbird 실험 하네스
│   ├── bimamba_model.py        BiMambaDispNet (131,878 params)
│   ├── eval_full_rollout.py    전체 궤적 롤아웃 (§6)
│   ├── stationary_baseline.py  정지 기준선 (§3.4)
│   ├── euroc_data.py           EuRoC 로더 + body 프레임 정렬 (§5.1)
│   ├── finetune_transfer.py    4개 arm, 동결/BN 고정, 롤아웃 ATE 선택
│   ├── transfer_eval.py        zero-shot
│   ├── reeval_transfer_full.py 전 런 재채점
│   ├── run_transfer_matrix.sh  매트릭스 실행
│   ├── summarise_transfer.py   시드 평균 집계
│   ├── plot_data_efficiency.py §5.3 그림
│   ├── diagnose_observability.py  §10.1/§10.2
│   ├── analyze_closedloop.py   §10.3 chase_ratio
│   ├── analyze_uncertainty.py  §10.6
│   └── verify_frames.py        좌표계 검증
├── mario_ros/                  PX4 연동 (프레임 어댑터, 추론 서버, 오도메트리 노드)
└── results/
    ├── final_comparison.json       §4 최종 비교
    ├── full_rollout.json           §3.3 NM 전체 궤적
    ├── motor_control_full_rollout.json  §3.3 모터 ON 대조군
    ├── stationary_baseline.json    §3.4 정지 기준선
    ├── airio_tde.json              §4 AirIO TDE
    ├── inference_latency_{m,mb}.json  §9.1
    ├── all_runs.{csv,json}         73개 런 전수
    ├── continuous_run{,_yf}.json   §10.5
    ├── stage6_sweep_analysis.json  §10.4
    └── transfer/
        ├── full_rollout_rescore.json  §5.2/§5.3 전 런
        ├── yaw_sweep_m_s42.json       §5.1 요 스윕
        └── euroc_dt_check.json        §5.1 샘플레이트

runs/
├── nm_s{42,1,2,3}   NM — 현 브랜치에서 로드 가능한 유일한 계열
├── m_s{42,1,2,3}    M  — 모터 인코더 포함, 현 브랜치에서 로드 불가
├── mb_s{42,1,2,3}   MB — 양방향, 현 브랜치에서 로드 불가
└── b8_val_*, b8_rot_*, c1_*, c2_*, h_*   기각된 가설들 (§8)
```

**체크포인트 호환성**: 모터 시대의 체크포인트(trial8, `m_*`, `mb_*`, `b*`)는 새 모델
클래스로 읽히지 않는다. 현 브랜치에서는 `runs/nm_s{42,1,2,3}`만 사용 가능하다.

---

## 13. 아직 하지 않은 것

1. **NM 체크포인트의 SITL 폐루프 검증** — `mario_infer_server.py` 기본값을
   `runs/nm_s42`로 바꿨으나 실제로 돌려보지 않았다. 배포 전 필수.
2. **꼬리 구간을 포함한 재평가** (§4.3) — 마지막 윈도우를 시퀀스 끝에 붙이면 커버리지가
   100 %가 되고, seen 비교가 AirIO와 정확히 같은 구간이 된다.
3. **NM 기준의 EuRoC 전이 재산출** (§5 주의) — 현재 수치는 전부 M 기준.
4. **PX4 4채널 모터 입력 실험** — `dds_topics.yaml`에 `/fmu/out/actuator_motors`를
   추가하고 99.194 Hz 발행을 확인했으나(PX4 재빌드 완료, 별도 저장소라 미커밋)
   학습에 쓰지 않았다. §3.5의 미해결 질문에 직접 답할 수 있는 실험.
5. **실기 이전** — GT는 GPS 도플러 속도만 쓸 것(위치 미분은 90 ms 라벨에 11–33 m/s
   잡음), `vehicle_local_position`은 EKF2 출력이라 순환 참조가 되므로 금지.
   MARIO는 FC에서 못 돌고 보조 컴퓨터(Jetson)가 필요하며 `EKF2_EV_DELAY` 재측정 필요.
