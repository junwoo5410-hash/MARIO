# Install PyTorch matching your CUDA version first, e.g.
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
torch>=2.0
numpy>=1.23
scipy>=1.9
pypose>=0.6
PyYAML>=6.0
matplotlib>=3.6
Pillow>=9.0

# Mamba requires a CUDA toolchain; install after torch.
mamba-ssm>=1.2
causal-conv1d>=1.2
