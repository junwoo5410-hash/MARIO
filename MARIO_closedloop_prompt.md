# MARIO 폐루프 항법 검증 — PX4 SITL

Claude Code 작업 지시서. 전체를 붙여넣어 사용한다.

---

## 0. 최종 목표

**GPS를 끈 PX4 SITL에서, MARIO의 위치 추정만으로 드론이 지정한 좌표까지 날아가는 것을 검증한다.**

구체적 성공 조건:

```
1. 이륙 후 고도 3 m 호버링
2. "북쪽 5 m 이동" 명령
3. Gazebo GT 기준 목표점 반경 1 m 이내 도달
4. 도달 후 30초 호버링, GT 드리프트 2 m 이내
```

이 조건을 만족하면 성공. 만족하지 못하면 **어느 단계에서 무너졌는지**를 규명하는 것이 그다음 목표다.

---

## 1. 시스템 구성

```
                    ┌─────────────────────────────┐
                    │  Offboard 노드               │
                    │  목표 좌표 → setpoint 발행    │
                    └──────────┬──────────────────┘
                               │ /fmu/in/trajectory_setpoint
                               ▼
  IMU ──────────────────► ┌─────────┐ ──► 위치제어 ──► 자세제어 ──► 모터
  기압계 ─────────────────►│  EKF2   │
  MARIO 속도 추정 ────────►└─────────┘
        ▲                  /fmu/in/vehicle_visual_odometry
        │
  ┌─────┴──────────────────┐
  │ MARIO 노드              │
  │ IMU + 자세 → 변위 추정   │
  └────────────────────────┘

  GPS: 비활성화
  Gazebo GT: 검증용으로만 기록, 제어에 사용 금지
```

---

## 2. MARIO 파악

`MARIO/`는 학습 기반 관성 항법 패키지다. **제어기가 아니라 상태 추정기다.**

- **입력**: `acc(3)`, `gyro(3)`, `rot_so3(3)`, `motor(3)` — 각 `(B, 1000, 3)`, 100 Hz
- **출력**: `disp(B, 112, 3)`, `cov(B, 112, 3)` — 바디 프레임 변위와 공분산
  - **주의**: 인코더는 1000샘플에서 **112** 스텝을 낸다. 라벨은 111개이고 `train.py:27`이
    앞 111개로 잘라 쓴다. 노드도 **앞 111개만** 사용할 것. 112번째는 윈도우 밖
    (입력 1004~1013)을 가리킨다. — Stage 0 실측 확인
- 9 샘플(0.09 s)마다의 변위를 예측
- 체크포인트: `runs/trial8_100ep/best.pt`
- 학습 데이터: MIT Blackbird (실내 모캡, 특정 기체) — **SITL 기체와 다르다**
- 참고 성능: SEEN ATE 0.479 m / UNSEEN ATE 1.557 m (eval 10궤적 평균 1.018 m)

### 고정 상수 (변경 금지)

```python
WINDOW_SIZE       = 1000    # 10초 @ 100Hz
DT                = 0.01
LABEL_START_INDEX = 14
LABEL_STRIDE      = 9
N_LABELS          = 111     # (1000-2)//9 + 1  (모델 출력 112 중 앞 111개)
```

```python
disp, cov = net(acc, gyro, rot_so3, motor)   # 모두 (B,1000,3) float32 → (B,112,3), 앞 111개 사용
```

`rot_so3`는 `pypose.SO3` 객체에 `.Log().tensor().float()` 적용.

---

## 3. 폐루프 설계 결정 — 반드시 이대로 할 것

아래 세 가지는 "되는 것"과 "즉시 발산하는 것"을 가른다. 임의로 바꾸지 말 것.

### 결정 A — 적분을 노드에서 하지 않고 EKF2에 맡긴다

MARIO 출력은 0.09초 구간의 바디 프레임 변위다. 이를 누적하면 위치가 되지만, **그 덧셈을 어디서 하느냐**가 폐루프 안정성을 좌우한다.

```
[A] 노드에서 누적 → position 필드로 발행   ← 채택하지 않음
[B] 스텝 변위만   → velocity 필드로 발행   ← 채택
```

두 방식 모두 위치를 만들어내고 궤적도 수치적으로 유사하다. 차이는 **공분산**에 있다.

`VehicleOdometry`는 값과 함께 그 값의 불확실성을 요구한다.

- **[A]를 택하면** N번째 스텝의 위치 공분산을 노드가 직접 계산해 넣어야 한다. 변위 오차가 누적된 값이므로 스텝 수에 따라 증가하며, 이를 제대로 전파하려면 별도의 필터가 하나 더 필요하다. 고정값으로 대충 채우면 **EKF2가 위치를 실제보다 신뢰하게 되어**, 수 미터 드리프트가 발생한 상황에서도 자체 공분산을 낮게 유지한다. 폐루프에서 가장 위험한 실패 모드다.
- **[B]를 택하면** 모델의 `cov` 헤드 출력을 그대로 쓸 수 있다. 그것은 **한 스텝 변위의 불확실성**이므로 누적 계산이 필요 없다. EKF2가 적분하면서 위치 공분산을 스스로 증가시키고, 결과적으로 "시간이 지날수록 위치 추정이 부정확해진다"는 사실을 필터가 정확히 인지한 상태가 된다.

`cov` 헤드는 원래 이 용도로 설계된 것이다. 이를 활용하는 것이 [B]의 근거다.

**구현**
```python
velocity_body = disp / (LABEL_STRIDE * DT)   # disp / 0.09 — 단위 변환일 뿐
```
`VehicleOdometry`의 velocity 필드를 채우고, position 필드는 NaN으로 비운다. `cov`를 velocity covariance에 매핑한다.

**예외**: loop closure, ArUco 마커 등 드리프트를 끊어주는 절대 기준이 도입되면 [A]가 적절해진다. 현재는 순수 dead-reckoning이므로 해당하지 않는다.

### 결정 B — 고도는 기압계에 맡긴다

MARIO는 z 변위도 예측하지만, 수직 드리프트는 곧바로 추락 또는 상승 폭주로 이어진다. `EKF2_HGT_REF`를 기압계로 두고, 외부 odometry는 수평 성분만 융합한다.

### 결정 C — 추정 지연을 EKF2에 신고한다

MARIO 추론에는 실측 가능한 지연이 있다. 이를 `EKF2_EV_DELAY`에 반영하지 않으면 필터가 부정합 상태가 되어 진동한다. Stage 2에서 측정한 실제 지연을 반드시 입력할 것.

**→ Stage 2 실측 완료** (`mario_sitl/results/stage2_shadow.json`, n=1996, 90 s 비행 중 측정):

| 지표 | 값 |
|---|---:|
| 달성 추론율 | **25.0 Hz** (목표 20 Hz 이상) |
| 종단 지연 p50 | **34.6 ms** |
| 종단 지연 p95 | **38.5 ms** (예산 50 ms) |
| 최대 | 69.9 ms (1996회 중 4회만 예산 초과) |
| 추론 자체 | 22.1 ms |

**→ `EKF2_EV_DELAY = 35` (ms)**. p50을 쓴다. p95는 꼬리 4건에 끌려가고, EKF2는 평균적
지연 정합을 원하므로 중앙값이 맞다. Stage 5 진입 전 파라미터 존재·단위를 재확인할 것.

**측정 정의**: IMU 샘플이 노드에 도착한 벽시계 시각 → 해당 속도값을 손에 쥔 시각. 소켓
왕복과 리샘플링을 포함한다. 즉 숨긴 비용이 없다.

---

## 4. 코드에서 확인된 함정

**함정 A — 모터 입력의 정체와, zeros 고정이 위험한 이유** *(2026-08-26 실측으로 개정)*

헤더는 `thrust_1..thrust_4`라고 하지만 실제 행에는 값이 3개뿐이다. 값으로 확인한 정체:

| 채널 | 정체 | 값 |
|---|---|---|
| ch0, ch1 | 바디 x/y 비추력 | **전 궤적에서 항상 정확히 0** |
| ch2 | 바디 z **질량 정규화 집합추력** [m/s²] | −13.0 ~ −9.2 |

개별 모터 추력이 아니라 **바디 프레임 비추력 벡터**다. `corr(ch2, acc_z) = +0.92`,
평균 차이 0.16 m/s² — 즉 ch2는 IMU의 `acc_z`와 사실상 같은 신호다.

**zeros 고정은 채택하지 않는다.** Blackbird 전 시퀀스에 추력 파일이 있어서 모델은 학습 중
`motor = (0, 0, ≈−0.82)`만 보았다. zeros는 중립적 ablation이 아니라 **분포 이탈**이다.
eval 10궤적 실측 (`mario_sitl/scripts/probe_motor_input.py`):

| 방식 | 평균 ATE | 원본 대비 |
|---|---:|---:|
| 원본 (추력 사용) | 1.018 m | — |
| zeros | **10.391 m** | **+921 %** |
| ch2 = 상수 −0.82 | 1.052 m | +3.4 % |
| **ch2 = clip(acc_z / 13, −1, 1)** | **0.968 m** | **−4.9 %** |

**→ `motor[:, 2] = clip(acc_z / 13.0, −1, 1)`, 나머지 두 채널은 0으로 고정한다.**

```python
ACCZ_SCALE = 13.0                                    # 학습 시 |비추력| 최대치
motor = torch.zeros(N, 3)
motor[:, 2] = (acc[:, 2] / ACCZ_SCALE).clamp(-1, 1)  # 인과적, IMU만으로 계산
```

추력 토픽을 구독할 필요가 없으므로 모터 매핑 모호성은 그대로 회피된다.

**함정 B — 모터 정규화가 비인과적**
```python
motor = motor / (np.max(np.abs(motor), axis=0) + 1e-8)
```
시퀀스 전체의 max로 나눈다. 스트리밍에서 재현 불가능.
**→ 함정 A의 고정 스케일(`ACCZ_SCALE = 13.0`)이 이 문제도 함께 해결한다.** 재학습 불필요.

**함정 C — 학습 시 자세가 GT였다**
`rollout_trajectory()`는 `rot_so3`에 GT 자세를 넣는다. 폐루프에서는 EKF2 추정 자세를 넣을 수밖에 없다. GPS가 없어도 IMU + 자력계로 자세는 유지되므로 동작은 하지만, 학습 시와 분포가 다르다.

**함정 D — 좌표 변환이 Blackbird 전용**
```python
R_W_NED = [[1,0,0],[0,-1,0],[0,0,-1]]
R_B_I   = [[0,-1,0],[1,0,0],[0,0,1]]
```
PX4는 NED 월드 + FRD 바디다. 그대로 쓰면 안 된다.

**→ Stage 3에서 유도·검증 완료** (`mario_sitl/scripts/verify_frames.py`).
MARIO 규약은 실측 결과 **월드 = NWU(z UP), 바디 = (x Right, y Back, z Down)** 이다.

```python
C_W = [[1,0,0],[0,-1,0],[0,0,-1]]    # NED -> NWU   (= 기존 R_W_NED)
C_B = [[0,1,0],[-1,0,0],[0,0,1]]     # FRD -> B_M   (= 기존 R_B_I 의 전치)

acc_M  = C_B @ acc_FRD                       # gyro 도 동일
R_M    = C_W @ R_ned_frd @ C_B.T             # .Log() -> rot_so3
disp_NED = R_ned_frd @ C_B.T @ disp_M        # 출력 되돌리기 (월드 홉은 상쇄됨)
```

실제 비행 데이터로 왕복 검증 시 최대 오차 2.4e-07 m.

**함정 E — 추론 비용**
현재 구현은 매 추론마다 1000 샘플 전체를 다시 처리한다. Mamba는 causal이라 원리적으로 상태 캐싱 O(1) 증분 추론이 가능하지만 이 코드는 그렇게 되어 있지 않다. Stage 2에서 실측 후 판단.

---

## 5. 단계별 작업

각 단계는 **다음 단계의 실패 원인을 좁히기 위한 것**이다. 순서를 지킬 것.

---

### Stage 0 — 환경 게이트

```bash
python -c "import torch; print(torch.__version__, torch.cuda.is_available())"
pip install mamba-ssm
python -c "from mamba_ssm import Mamba; print('ok')"
```

`best.pt` 로드 후 `(1,1000,3)` 더미 4개 → `(1,111,3)` 두 개 반환 확인.

**GPU가 없으면 여기서 중단하고 보고할 것.** `mamba-ssm`은 CUDA 툴체인이 필요하다. 임의로 우회하지 말 것.

**산출물**: `scripts/check_env.py`

---

### Stage 1 — 제어 뼈대 (MARIO 없이)

**목적**: 폐루프 실패 시 "제어 코드 탓인지 MARIO 탓인지" 구분할 기준선을 만든다.

**작업**
1. PX4 SITL + ROS 2 + uXRCE-DDS 환경 구축
   ```bash
   make px4_sitl gz_x500
   MicroXRCEAgent udp4 -p 8888
   ```
2. Offboard 노드 작성 — **GPS를 켠 정상 상태에서**:
   - setpoint를 10회 이상 선발행 후 Offboard 모드 진입 (순서를 지키지 않으면 모드 전환이 거부된다)
   - Arm → 고도 3 m 이륙
   - 북쪽 5 m 이동 → 30초 호버링 → 착륙
   - setpoint는 최소 50 Hz로 지속 발행 (2 Hz 미만이면 페일세이프로 모드 이탈)
3. PX4는 NED를 쓴다. 고도 3 m는 `z = -3.0`이다.

**성공 기준**: GT 기준 목표점 0.3 m 이내 도달. 이 상태가 이후 모든 비교의 기준선이다.

**산출물**: `mario_ros/offboard_mission_node.py`

---

### Stage 2 — MARIO 실시간 노드 (섀도 모드)

**목적**: MARIO를 돌리되 **제어에는 전혀 관여시키지 않고** 실시간 성능만 측정한다.

**구독**

| 토픽 | 필드 |
|---|---|
| `/fmu/out/sensor_combined` | `accelerometer_m_s2`, `gyro_rad` |
| `/fmu/out/vehicle_attitude` | `q` |
| `/fmu/out/vehicle_local_position` | GT 비교용 |

Gazebo GT는 `vehicle_local_position_groundtruth` / `vehicle_attitude_groundtruth` uORB 토픽으로 접근한다. DDS 브리지 설정(`dds_topics.yaml`)에 없으면 추가할 것.

**작업**
1. 1000 샘플 링버퍼 유지, 100 Hz 균일 그리드 보간
   - `data.py`의 `interp1d` 방식 준수
   - 쿼터니언은 반드시 `Slerp` (선형 보간 금지)
2. 추론 → 마지막 스텝 변위를 `disp / 0.09`로 속도 단위 변환 (결정 A). 노드에서 누적하지 않는다.
3. **발행하지 않고 로깅만 한다**
4. **지연 시간 측정**: IMU 샘플 도착 → 추정치 산출까지의 실제 경과 시간. 히스토그램으로 기록.

**성공 기준**
- 안정적으로 최소 20 Hz 추론 유지
- p95 지연 50 ms 이내

**실패 시**: 추론 주기를 낮추거나(10 Hz) Mamba 상태 캐싱 리팩터링을 검토. **여기서 실시간성이 안 나오면 폐루프는 불가능하다.** 임의로 진행하지 말고 보고할 것.

**산출물**: `mario_ros/mario_odometry_node.py`, 지연 히스토그램

---

### Stage 3 — 좌표계 검증

**이 단계를 건너뛰면 이후 모든 실패의 원인을 알 수 없게 된다.**

**방법**: 모델을 쓰지 않는다. GT 변위를 그대로 왕복 변환한다.

```python
world_disp = gt_pos[t+stride] - gt_pos[t]
body_disp  = gt_rot[t].Inv() @ world_disp    # dataset.py와 동일
recovered  = gt_rot[t] @ body_disp           # 다시 월드로
```

**성공 기준**: 복원 궤적 ATE < 1e-4 m (수치 오차 수준)

**실패 시**: 좌표 변환 또는 인덱싱 오류다. 모델을 의심하지 말고 여기부터 해결한다. `R_W_NED`, `R_B_I`에 대응하는 PX4용 변환을 유도하고, 근거를 주석으로 남길 것.

**산출물**: `scripts/verify_frames.py`

---

### Stage 4 — 개루프 정확도 측정

**목적**: 폐루프에 넣기 전, MARIO가 SITL 기체에서 어느 정도 오차를 내는지 안다.

Stage 2에서 기록한 데이터를 `mario.evaluate.rollout_trajectory()`에 투입해 ATE / TDE 산출.

**결과 해석**

| ATE | 폐루프 전망 | 조치 |
|---|---|---|
| < 0.5 m | 성공 가능성 있음 | Stage 5 진행 |
| 0.5–2 m | 목표점 도달은 되나 호버 유지 불안 | Stage 5 진행하되 기대치 조정 |
| > 2 m | 폐루프에서 발산 가능성 높음 | SITL 데이터로 fine-tuning 검토 |

**주의**: SITL IMU에는 실제 프로펠러 진동이 없어 실기보다 깨끗하다. 여기 결과가 실기 성능을 보장하지 않는다.

**산출물**: `scripts/eval_openloop.py`, 결과 JSON, 궤적 플롯

---

#### Stage 4 실측 결과 (2026-08-26) — **ATE 5.2 m, 표의 ">2 m" 구간**

150 s 2구간 비행 (`results/stage4_flight.npz`), 10.2 s 세그먼트 14개:

| 자세 입력 | 평균 ATE | 최대 | TDE |
|---|---:|---:|---:|
| GT 자세 (학습과 동일) | **5.193 m** | 6.922 m | 96.8 % |
| EKF 자세 (폐루프 조건) | 5.273 m | 6.801 m | 99.4 % |

**함정 C의 대가는 0.08 m로 무시할 수준이다.** 자세 소스는 문제가 아니다.

**진짜 원인 — 학습 분포에 저속 구간이 없다.**

정지 상태에서 MARIO가 **0.69–1.06 m/s의 유령 속도**를 출력한다. 기체가 땅에 서 있는
세그먼트의 ATE가 3.8–4.5 m로, 실제 비행 세그먼트와 거의 같다. 즉 오차가 속도와 무관한
**상수 바이어스**다.

Blackbird eval 10궤적 속도 분포 실측:

| | min | p05 | mean | max | <0.2 m/s 비율 |
|---|---:|---:|---:|---:|---:|
| 전체 | 0.178 | 0.842 | 2.946 | 7.708 | **0.03 %** |

학습 데이터의 기체는 **항상 빠르게 날고 있다**. 정지·호버는 완전한 분포 밖이고, 모델은
"Blackbird 드론은 대략 이 속도로 움직인다"는 사전분포를 출력 바닥값으로 학습했다.
Stage 2의 `vel_rmse_vs_gt = 0.98 m/s`가 같은 현상을 독립적으로 가리킨다.

**폐루프 전망**: 우리 참조 미션은 평균 속도 0.17 m/s로 대부분 호버·정지다. 호버 중
EKF에 약 1 m/s의 유령 속도를 계속 주입하면 제어기가 이를 쫓아가 발산한다.
**표의 처방대로 fine-tuning 없이 Stage 5를 진행하면 실패가 예정되어 있다.**

> **[2026-08-26 정정] 위 진단은 부분적으로 틀렸다.** 진짜 원인은 기체나 커버리지가 아니라
> **관측 가능성**이었다. 아래 "Stage 4c" 참조.

---

#### Stage 4c — 진짜 원인: IMU가 속도를 담고 있는가

결정적 실험: SITL로 학습한 모델을 **자기 자신의 held-out 비행**에 평가했더니
`cos = −0.002`. 도메인 격차로는 이렇게 못 나온다. 데이터 자체에 학습할 신호가 없었다.

IMU가 속도를 드러내는 경로는 둘뿐이다 — **과도 동역학**(적분 가능한 가속도)과
**정상상태 항력**(속도에 비례하는 수평 비추력). 등속 비행은 첫째를 내놓지 않는다.
그런데 내 `varied_flight_node`는 셋포인트를 일정 속도로 **걸어서** 옮기고 있었다.
깨끗한 등속 비행 = 속도 정보 없음.

| 데이터 | 속도 | \|accel\| | >1 m/s² | 항력 신호 |
|---|---:|---:|---:|---:|
| Blackbird | 2.7–5.4 | **4.6–6.8** | **98–100 %** | 1.07–1.86 |
| SITL (걷기 방식) | 1.2 | **0.42–0.67** | **13–26 %** | 0.13–0.18 |
| SITL (스텝 방식) | 4.3–4.7 | **4.4–4.9** | **88–97 %** | 0.46–0.53 |

**Blackbird 내부에서도 같은 법칙이 성립한다** (동일 기체, held-out):

| 시퀀스 | \|accel\| | 항력 | cos |
|---|---:|---:|---:|
| maxSpeed2p0 | 1.89 | 0.524 | +0.828 |
| maxSpeed3p0 | 3.18 | 1.080 | +0.977 |
| maxSpeed5p0 | 5.33 | 1.097 | +0.991 |

**기체가 아니라 비행 영역이 지배 변수다.**

**공격적 데이터로 재학습 결과** (`runs/sitl_agg`, held-out `flight_6`):

| | cos | 축별 x/y/z | TDE |
|---|---:|---|---:|
| 기존(걷기) 학습 | −0.002 | +0.19/−0.02/+0.20 | 56.5 % |
| **공격적 학습** | **+0.964** | **+0.99/+0.99/+0.97** | **9.70 %** |
| Blackbird 참고 | +0.83 – +0.99 | — | 7.99 % |

항력이 여전히 2배 약한데도 학습됐다. **동역학이 구속 조건이었다.**

---

#### Stage 5 재시도 — 날았지만 18.7 m 흘렀다

`Armed → Takeoff detected → **Disarmed by landing**` (추락 아님, 정상 착륙).

| 지표 | GPS 기준선 | MARIO 폐루프 |
|---|---:|---:|
| chase_ratio | 1.25 | **0.94** |
| 정착 중 기체 이탈 | **0.59 m** | **18.70 m** |
| 추정 오차 (전/후반) | 0.12 → 0.38 m | 5.24 → 10.33 m |
| 발산 | False | **True** (0.32 m/s) |

`chase_ratio 0.94`는 기체 이탈이 추정 오차의 **정확히 반대 방향, 같은 크기**라는 뜻이다.
제어기가 틀린 추정에 현실을 맞췄다. 메커니즘은 GPS 기준선과 동일하고(둘 다 필터를 따름)
**크기만 30배**다.

**남은 근본 한계**: 호버에서는 속도가 원리적으로 관측 불가능하다. 항력 신호가 속도에
비례하므로 v≈0에서 신호도 0이고 잡음과 구분되지 않는다. **IMU만으로 호버를 dead-reckoning
하는 것은 ill-posed 문제이며, 학습량으로 해결되지 않는다.** 절대 기준(loop closure,
마커, 기압계 이상의 무언가)이 필요하다.

**방법론 주의 — `rollout_trajectory`는 첫 윈도우만 적분한다.**
`window_size % label_stride = 1000 % 9 = 1`이라 윈도우 k의 라벨 인덱스가 `(5+k) mod 9`로
어긋나 이전 윈도우의 `positions`에 절대 연결되지 않는다. 실측: 입력 14994 샘플 → 113 포즈
(10.2 s). `mario/`는 수정 금지(작업 규칙 3)이므로 본 스크립트는 비행을 10.2 s 세그먼트로
잘라 함수를 세그먼트마다 호출한다. **Blackbird ATE 수치(0.479 / 1.557 m)도 전체 궤적이
아니라 10 s 세그먼트 기준이라는 뜻이다.**

---

### Stage 5 — 폐루프 (본 목표)

**PX4 파라미터**

```
EKF2_GPS_CTRL  = 0          # GPS 융합 비활성화
EKF2_EV_CTRL   = <속도 비트만>  # 외부 odometry 속도만 융합 (결정 A, B)
EKF2_HGT_REF   = <기압계>     # 고도는 기압계 (결정 B)
EKF2_EV_DELAY  = <Stage 2 실측값 ms>   # (결정 C)
```

**주의**: 파라미터 이름과 비트마스크 값은 PX4 버전에 따라 다르다. **실제 파라미터 목록에서 확인하고 각 값의 의미를 주석으로 남길 것.** 추측으로 작성 금지.

**→ 검증 완료** (출처: `PX4-Autopilot/src/modules/ekf2/module.yaml`, v1.15 실제 정의):

| 파라미터 | 타입 | 기본 | 설정값 | 근거 |
|---|---|---:|---:|---|
| `EKF2_GPS_CTRL` | bitmask(0–15) | 7 | **0** | 비트 0 위경도 / 1 고도 / 2 3D속도 / 3 듀얼안테나. 전부 끔 |
| `EKF2_EV_CTRL` | bitmask(0–15) | 15 | **4** | 비트 2 = 3D velocity **만**. 비트 0·1(위치)은 끔 → 결정 A |
| `EKF2_HGT_REF` | enum | 1 (GPS) | **0** | 0 = Barometric pressure → 결정 B. **재부팅 필요** |
| `EKF2_BARO_CTRL` | boolean | 1 | **1** | 기압계 고도 보조 유지 (기본값 그대로) |
| `EKF2_EV_DELAY` | float, ms (0–300) | 0 | **35** | Stage 2 실측 p50 34.6 ms → 결정 C. **재부팅 필요** |

`EKF2_HGT_REF`와 `EKF2_EV_DELAY`는 `reboot_required: true`이므로 **설정 후 PX4를 재시작**해야
적용된다. 실행 순서: 파라미터 설정 → 재시작 → 미션.

**작업**
1. Stage 2 노드가 `/fmu/in/vehicle_visual_odometry`로 발행하도록 전환
   - `cov` 헤드 출력을 covariance 필드에 매핑 — 그 헤드의 설계 의도다
2. Stage 1의 미션을 GPS 없이 재실행
3. 전 구간에서 GT와 EKF2 추정을 동시 기록

**측정 항목**

| 항목 | 정의 |
|---|---|
| 도달 오차 | 목표점과 GT 최종 위치의 거리 |
| 추정 오차 | EKF2 추정 위치와 GT의 차이 (시간축 전체) |
| 호버 드리프트 | 30초 호버 동안 GT 위치 변화량 |
| 발산 여부 | 추정 오차가 단조 증가하는가 |

**해석 지침**

폐루프에서는 추정 오차가 제어 입력으로 되먹임된다. 다음 두 가지를 구분해서 보고할 것:

- **추정이 틀렸는데 기체는 제자리**: 제어기가 아직 반응 못 함. 초기 증상.
- **추정 오차만큼 기체가 실제로 이동**: 제어기가 틀린 추정을 믿고 보정한 것. 이것이 폐루프 고유의 실패 양상이며, 개루프 평가로는 절대 드러나지 않는다.

**호버링이 직진보다 어렵다.** 정지 상태에서는 변위가 0에 가깝고 예측은 노이즈만 남아, 적분하면 랜덤워크가 된다. 제어기가 그 노이즈를 쫓아다니며 기체가 서서히 흘러간다. 30초 호버 구간을 반드시 포함할 것.

**산출물**: `results/closedloop_run_*.json`, 시간축 오차 플롯, 3D 궤적 비교 플롯

---

#### Stage 5 실측 결과 (2026-08-26) — **무장 성공, 약 2초 비행, 추락**

**인프라 게이트는 통과했다.** `EKF2_GPS_CTRL=0` + MARIO 속도 전용 EV 융합만으로
`xy_valid: true`, `v_xy_valid: true`가 뜬다. 즉 **속도만 융합해도 PX4는 위치를 유효로
선언하고 오프보드 무장이 가능하다.** 실시간 성능도 발행 상태에서 유지된다
(25 Hz, p95 37.9 ms, 실패 0). 파라미터도 재시작 후 유지됨을 실제 재시작으로 확인했다.

**비행 결과** (`results/closedloop_run_mario.json`, `stage5_divergence.json`):

```
Armed by external command -> Takeoff detected -> Failsafe activated -> Disarmed
```

| t | GT 위치 | EKF 변위 | 오차 |
|---:|---|---:|---:|
| 6 s | 이륙, D −0.47 | 2.96 m | 1.91 m |
| 8 s | **접지, 정지** | 18.76 m | 17.19 m |
| 30 s | 정지, 롤 −63.5° | 399.71 m | 398.13 m |

이륙 시점에 이미 추정 오차가 1.91 m였고 발산 중이었다. 제어기는 추정값을 향해
보정했고, 그 보정이 기체를 땅에 처박았다. **지시서가 구분하라고 한 "추정 오차만큼
기체가 실제로 이동" 유형이 맞다.** 다만 예상했던 호버 중 완만한 드리프트가 아니라
2초 만의 즉시 추락이었다.

**주의 — 8초 이후 수치는 결과가 아니라 사후 현상이다.** 옆으로 누운 기체(롤 −63.5°)의
가속도계는 `(−0.57, 8.36, −4.63)`을 읽는다. 학습 분포에 전혀 없는 입력이고 모델은
외삽해 24 m/s를 뱉는다. EKF가 400 m까지 달아난 것은 추락의 **결과**다.

**fine-tuning 4종 모두 폐루프 기준에 못 미쳤다:**

| 변형 | 이동 구간 ATE | 정지 드리프트 |
|---|---:|---:|
| 기준선 (fine-tune 없음) | 5.193 m | 4.238 m |
| Blackbird + SITL 혼합 | 4.222 m | **1.026 m** |
| SITL 전용 | **4.121 m** | 1.225 m |
| SITL 속도 균형 | 5.166 m | 2.178 m |

최선이 4.121 m로 여전히 ">2 m, 발산 가능성 높음" 구간이다. 병목은 하이퍼파라미터가
아니라 **데이터 양**이다: `step_size=3`이라 인접 윈도우가 1000 중 997 샘플을 공유하므로
4분 6회 비행에는 독립 윈도우가 약 140개뿐이고, SITL 전용 학습은 epoch 1에서 최고
test RMSE를 찍은 뒤 59 epoch 동안 악화됐다(과적합).

**드래그 계수가 3배 다르다** — 속도 관측성의 근원:

| | 드래그 기울기 (m/s²)/(m/s) |
|---|---:|
| Blackbird (실기) | 0.327 – 0.405 |
| SITL Gazebo x500 | 0.095 – 0.149 |

정지 비행 시 속도를 인코딩하는 유일한 신호가 항력에 의한 수평 비추력인데, SITL에서는
그 신호가 3배 약하다. Blackbird로 보정된 모델은 약한 신호를 낮은 속도로 읽는다
(실측 순항 과소추정 40 %). 두 도메인을 섞어 학습하면 서로 모순되는 보정을 요구하므로
**혼합은 이 목표에 부적절했다** (Blackbird SEEN 0.479 → 0.607, UNSEEN 1.557 → 2.158 퇴행).

---

### Stage 6 — 한계 특성화

Stage 5가 성공하든 실패하든 진행한다. 연구 결과물은 여기서 나온다.

**실험 격자**

| 변수 | 값 |
|---|---|
| 이동 거리 | 2, 5, 10, 20 m |
| 이동 속도 | 0.5, 1.0, 2.0 m/s |
| 호버 시간 | 10, 30, 60, 120 초 |

**규명할 것**
- 도달 오차가 거리에 따라 어떻게 증가하는가 (선형? 초선형?)
- 호버 드리프트가 시간에 따라 어떻게 증가하는가
- 폐루프가 발산하기 시작하는 임계점은 어디인가
- `cov` 출력이 실제 오차와 상관관계가 있는가 — **불확실성 추정이 유의미한지 검증**

**산출물**: `REPORT.md` — 각 실험의 수치와 해석

---

## 6. 산출물 구조

```
mario_sitl/
├── scripts/
│   ├── check_env.py            # Stage 0
│   ├── verify_frames.py        # Stage 3
│   ├── eval_openloop.py        # Stage 4
│   └── run_sweep.py            # Stage 6
├── mario_ros/
│   ├── offboard_mission_node.py    # Stage 1
│   └── mario_odometry_node.py      # Stage 2, 5
├── launch/
│   ├── baseline_gps.launch.py      # Stage 1
│   └── closedloop_mario.launch.py  # Stage 5
├── results/
│   ├── latency_hist.png
│   ├── frames_verification.json
│   ├── eval_openloop.json
│   ├── closedloop_run_*.json
│   └── figures/
└── REPORT.md
```

---

## 7. 작업 규칙

1. **Stage 0 → 6 순서를 지킨다.** 각 단계의 성공 기준 미충족 시 다음으로 넘어가지 않는다.
2. **Stage 1과 Stage 3을 건너뛰지 않는다.** 폐루프가 실패했을 때 원인을 좁힐 유일한 기준선이다.
3. `MARIO/mario/` 아래 기존 코드는 **수정하지 않는다.** 새 파일로 감싼다. 불가피하면 이유를 설명하고 승인을 받는다.
4. PX4 파라미터 이름과 값을 **추측으로 작성하지 않는다.** 실제 파라미터 목록이나 공식 문서에서 확인하고 근거를 주석으로 남긴다.
5. 결과가 나쁠 때 파라미터나 임계값을 조정해 숫자를 맞추지 않는다. 원인 분석이 우선이다.
6. 각 단계 완료 시 수치를 명시해 보고한다.
7. **폐루프가 실패해도 그것은 유효한 결과다.** 어떤 조건에서 무너지는지를 정확히 기록하는 것이 목표다.

---

## 8. 시작 전 확인

- [ ] `torch.cuda.is_available()`
- [ ] `mamba-ssm` 설치 성공 여부
- [ ] ROS 2 배포판 (Humble / Jazzy)
- [ ] PX4-Autopilot 버전
- [ ] `best.pt` 더미 추론 성공 여부

하나라도 실패하면 그 해결을 최우선으로 하고 보고할 것.
